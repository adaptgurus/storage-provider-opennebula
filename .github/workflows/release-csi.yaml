---
name: Release

on:
  push:
    tags: ["v*.*.*"]

permissions:
  contents: write
  packages: write

jobs:
  release:
    # LayerSentry has a separate qualification/release path. Never let a
    # vX.Y.Z-layersentry.N tag fall through the upstream-compatible publisher,
    # which builds a different image/profile and intentionally disables default
    # BuildKit attestations.
    if: ${{ !contains(github.ref_name, '-layersentry.') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: "${{ github.actor }}"
          password: "${{ secrets.GITHUB_TOKEN }}"

      - name: Build and push images
        run: make docker-release-opennebula-csi "CLOSEST_TAG:=$GITHUB_REF_NAME" BUILDX_NO_DEFAULT_ATTESTATIONS=1

      - name: Generate charts
        run: make helm-package "CLOSEST_TAG:=$GITHUB_REF_NAME"

      - name: Checkout repository (gh-pages)
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: ./gh-pages/

      - name: Deploy charts
        run: |
          shopt -s failglob
          cp -f ./_charts/$GITHUB_REF_NAME/opennebula-csi/opennebula-csi-${GITHUB_REF_NAME:1}.tgz ./gh-pages/charts/

      - name: Re-index charts
        working-directory: ./gh-pages/charts/
        run: |
          helm repo index ./ --url https://opennebula.github.io/storage-provider-opennebula/charts/

      - name: Push gh-pages
        working-directory: ./gh-pages/charts/
        run: |
          shopt -s failglob
          git add ./index.yaml ./opennebula-csi-${GITHUB_REF_NAME:1}.tgz
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"
          git commit -m "${GITHUB_REF_NAME:1}"
          git push

      - name: Create release
        env: { GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}" }
        run: gh release create "$GITHUB_REF_NAME" --generate-notes
