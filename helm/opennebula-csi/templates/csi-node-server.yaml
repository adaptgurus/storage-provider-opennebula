{{- include "opennebula-csi.validateDriverIdentityArgs" . -}}
{{- $driverName := include "opennebula-csi.driverName" . -}}
{{- $layersentryHealth := eq $driverName "csi.layersentry.io" -}}
---
kind: DaemonSet
apiVersion: apps/v1
metadata:
  name: {{ printf "%s-node" (include "opennebula-csi.fullname" .) }}
  namespace: {{ include "opennebula-csi.namespace" . }}
spec:
  updateStrategy:
    rollingUpdate:
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "opennebula-csi.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
      app.kubernetes.io/component: node
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "opennebula-csi.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: node
      annotations:
        kubectl.kubernetes.io/default-container: opennebula-csi
        {{- with .Values.node.podAnnotations }}
{{ toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "opennebula-csi.nodeServiceAccountName" . }}
      priorityClassName: system-node-critical
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.node.nodeSelector }}
      nodeSelector:
{{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.node.affinity }}
      affinity:
{{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.node.tolerations }}
      tolerations:
{{ toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: opennebula-csi
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if or .Values.debugPort .Values.metrics.driver.enabled }}
          ports:
            {{- if .Values.debugPort }}
            - containerPort: {{ .Values.debugPort }}
              name: debug
              protocol: TCP
            {{- end }}
            {{- if .Values.metrics.driver.enabled }}
            - containerPort: {{ .Values.metrics.driver.port }}
              name: driver-metrics
              protocol: TCP
            {{- end }}
          {{- end }}
          args:
            - "--endpoint=$(CSI_ENDPOINT)"
            - "--drivername={{ $driverName }}"
            - "--nodeid=$(NODE_ID)"
            - "--v={{ .Values.driver.logLevel }}"
            - "--vmodule=leaderelection=0"
            {{- with .Values.driver.extraArgs }}
{{ toYaml . | nindent 12 }}
            {{- end }}
            {{- with .Values.node.extraArgs }}
{{ toYaml . | nindent 12 }}
            {{- end }}
          env:
            - name: CSI_ENDPOINT
              value: unix:///csi/csi.sock
            - name: NODE_ID
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
{{ include "opennebula-csi.driverCommonEnv" (dict "context" . "inventoryEnabledOverride" false) | nindent 12 }}
            {{- with .Values.node.extraEnv }}
{{ toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.node.resources }}
          resources:
{{ toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            privileged: true
            capabilities:
              add:
                - SYS_ADMIN
            allowPrivilegeEscalation: true
          {{- if $layersentryHealth }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9808
            initialDelaySeconds: 20
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /healthz
              port: 9808
            initialDelaySeconds: 5
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          {{- end }}
          volumeMounts:
            - name: csi-socket-dir
              mountPath: /csi
            - name: pods-mount-dir
              mountPath: {{ include "opennebula-csi.kubeletRootDir" . }}
              mountPropagation: Bidirectional
            - name: device-dir
              mountPath: /dev
        - name: csi-node-driver-registrar
          image: {{ include "opennebula-csi.sidecarImage" (dict "context" . "name" "nodeDriverRegistrar" "default" "registry.k8s.io/sig-storage/csi-node-driver-registrar:v2.14.0") | quote }}
          {{- if .Values.metrics.enabled }}
          ports:
            - containerPort: {{ .Values.metrics.ports.registrar }}
              name: reg-metrics
              protocol: TCP
          {{- end }}
          args:
            - "--v=5"
            - "--vmodule=leaderelection=0"
            - "--csi-address=$(ADDRESS)"
            - "--kubelet-registration-path=$(DRIVER_REG_SOCK_PATH)"
            {{- if .Values.metrics.enabled }}
            - "--http-endpoint=:{{ .Values.metrics.ports.registrar }}"
            {{- end }}
          env:
            - name: ADDRESS
              value: /csi/csi.sock
            - name: DRIVER_REG_SOCK_PATH
              value: {{ printf "%s/csi.sock" (include "opennebula-csi.kubeletPluginDir" .) | quote }}
            - name: KUBE_NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          volumeMounts:
            - name: csi-socket-dir
              mountPath: /csi
            - name: registration-dir
              mountPath: /registration
        - name: liveness-probe
          imagePullPolicy: IfNotPresent
          image: {{ include "opennebula-csi.sidecarImage" (dict "context" . "name" "livenessProbe" "default" "registry.k8s.io/sig-storage/livenessprobe:v2.16.0") | quote }}
          args:
            - --csi-address=/csi/csi.sock
            - --probe-timeout=3s
            - --health-port=9808
          volumeMounts:
            - name: csi-socket-dir
              mountPath: /csi
      volumes:
        - name: csi-socket-dir
          hostPath:
            path: {{ include "opennebula-csi.kubeletPluginDir" . | quote }}
            type: DirectoryOrCreate
        - name: pods-mount-dir
          hostPath:
            path: {{ include "opennebula-csi.kubeletRootDir" . | quote }}
            type: Directory
        - name: device-dir
          hostPath:
            path: /dev
        - name: registration-dir
          hostPath:
            path: {{ include "opennebula-csi.kubeletRegistrationDir" . | quote }}
            type: DirectoryOrCreate